enum UserRole {
  ADMIN
  USER
}

enum Gender {
  MALE
  FEMALE
}

enum UserStatus {
  ACTIVE
  BLOCKED
  DELETED
}

enum TravelType {
  SOLO
  FAMILY
  FRIENDS
}

enum PaymentStatus {
  PENDING
  SUCCESS
  FAILED
}

enum SubscriptionPlan {
  MONTHLY
  YEARLY
  VERIFIED_BADGE
}

enum RequestStatus {
  PENDING
  ACCEPTED
  REJECTED
}

enum VisibilityType {
  PUBLIC
  PRIVATE
}
